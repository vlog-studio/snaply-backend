# 결정 요청 — 영상(스냅)을 서버에 얼마나 오래 보관할 것인가

**작성일**: 2026-09-05 (2026-09-09 결정)
**상태**: 결정 — 서버 원본은 **업로드 후 15일**에 만료된다(선택지 A). 이 기간 한도만 확정이며,
**구독자에게 기간 연장·무제한을 주는 것은 예정일 뿐 미확정**이다(A-2에서 결정).
아래 §선택지는 결정 당시의 비교 기록이며, 확정 내용은 §결정 기록이다.
**출처**: [meetings/2026-08-31-dev-sync.md](../meetings/2026-08-31-dev-sync.md) §4 "video (15일 지속)"
**관련 문서**: [specs/snap-library.md](../specs/snap-library.md) SNAP-9 · [storage-and-subscription-policy.md](storage-and-subscription-policy.md) §1·§2 ·
[snap-source-of-truth.md](snap-source-of-truth.md) §6 · [backlog.md](../backlog.md) A-1 ⑥ ·
후속 계획 [plans/2026-08-31-dev-sync-follow-up.md](../plans/2026-08-31-dev-sync-follow-up.md) T2

## 한 줄 요약

회의에서 "영상은 15일만 유지한다"는 제안이 나왔다. 그런데 지금 확정된 정책은 "기간 제한 없이,
무료 사용자는 총 2GB까지"다. 둘은 서로 다른 방식이라 하나를 고르거나 둘을 합치는 규칙을 정해야 한다.

## 쉬운 설명 — 지금은 어떻게 되어 있나

- 사용자가 3~5초 스냅을 찍으면 **앱이 자동으로 서버에 복사본을 올린다**. 서버 복사본은 백업이다.
- 서버 복사본은 **지우지 않는다**. 대신 "무료 사용자는 원본 합계 2GB까지"라는 **용량 한도**가
  결정돼 있다(SNAP-9). 한도를 넘으면 새 업로드를 막고 로컬에만 두는 방식이다. 이 한도 집행 코드는
  아직 없다.
- 보관 용량을 늘리는 것은 **구독 상품**으로 팔기로 했다(A-2 보관 축). 즉 "오래·많이 보관"이 돈을
  받는 근거다.

회의 제안은 "15일이 지나면 영상이 사라진다"는 **기간 한도**다. 기간 한도는 용량 한도와 성격이 다르다:
사용자가 아무것도 안 해도 영상이 없어진다.

## 이 결정이 영향을 주는 곳

| 영역 | 무엇이 바뀌나 |
|---|---|
| 서버 DB (`Video`) | 만료 시각 컬럼이 필요해진다. 만료분을 지우는 배치 작업이 새로 생긴다 |
| 무비(프로젝트) | 무비가 참조하는 스냅이 만료로 사라지면 그 컷을 잃는다(SNAP-5 와 같은 연쇄). 15일 안에 내보내지 않은 프로젝트가 깨질 수 있다 |
| 결제·구독 | "보관 용량을 구독으로 판다"는 A-2 설계의 근거가 약해지거나 바뀐다 |
| 앱 | 남은 보관 기간 표시, 만료 임박 알림, 만료된 스냅의 로컬 처리 규칙이 필요하다 |
| 문서 | SNAP-9 spec, 스토리지 정책 결정 문서, 약관(보관 기간 고지) |

## 선택지와 그 결과

### 선택지 A — 회의안: 15일 후 서버에서 만료

| 좋은 점 | 나쁜 점 |
|---|---|
| 서버 저장 비용이 가장 낮고 예측 가능하다 | 15일 안에 무비를 만들지 않은 사용자는 재료를 잃는다. "찍어두고 나중에 묶는" 사용 방식이 불가능해진다 |
| 2GB 한도 집행(유예 → 읽기 전용 전이) 구현이 필요 없어져 단순하다 | 보관 축 구독 상품의 판매 근거가 사라진다. A-2 를 다시 설계해야 한다 |
| | 로컬 삭제([local-copy-after-upload.md](local-copy-after-upload.md))까지 같이 하면 **사용자 영상이 완전히 소멸**한다. 약관 고지와 만료 전 알림이 필수다 |

### 선택지 B — 현행 유지: 기간 무제한 + 2GB 용량 한도

| 좋은 점 | 나쁜 점 |
|---|---|
| 이미 결정·문서화돼 있어 추가 결정이 없다 | 2GB 한도 집행 로직(현재 미구현)을 만들어야 한다 |
| 구독 상품 설계와 맞는다 | 사용자당 저장 비용이 계속 쌓인다(원가 계산은 스토리지 정책 §2) |
| 사용자가 언제든 예전 스냅으로 무비를 만들 수 있다 | |

### 선택지 C — 병행: 기본 15일, 구독자는 무제한(또는 더 긴 기간)

| 좋은 점 | 나쁜 점 |
|---|---|
| 무료 비용을 누르면서 구독 판매 근거를 유지한다 | 규칙이 두 개라 사용자에게 설명이 어렵고 앱 UI 가 복잡해진다 |
| | 구독이 끝나면 영상을 어떻게 할지(§4.4 유예 규칙)까지 다시 정해야 한다 |

## 권장

**B 를 기본으로 두고, A 를 원한다면 "만료 대상은 무비에 쓰이지 않은 스냅만" 같은 예외를 붙일지부터
검토**한다. 이유: 결제·보관 정책 3편이 이미 B 를 전제로 확정돼 있어, A 로 가면 구독 상품 설계까지
되돌아간다. 비용 절감이 목적이라면 원본 대신 배포 렌디션만 남기는 안(스토리지 정책 §6 에서 기각됨)을
다시 열어 비교하는 것이 더 정확하다.

## 결정 기록

| 항목 | 기록 |
|---|---|
| 결정 | **선택지 A — 기간 기준으로 전환한다.** 서버 원본은 업로드 후 **15일**에 만료된다. 이 값이 이번에 확정된 전부다 |
| 결정일 / 결정자 | 2026-09-09 / 제품 |
| 기각한 선택지 | **B(기간 무제한 + 2GB 용량 한도)** — 사용자당 저장 비용이 계속 쌓이고, 무료 사용자가 한도에 닿기까지 오래 걸려(월 50편 기준 1년 이상) 유료 전환의 경계가 되지 못한다 |
| **확정되지 않은 것** | **구독자에게 기간 연장·무제한을 주는 것은 예정일 뿐 미확정이다.** 그렇게 가면 선택지 C 의 형태가 되지만, 티어·기간·가격이 정해지기 전까지는 "15일 만료" 외에 어떤 예외도 확정된 바 없다. 결정 자리는 A-2(구독 상품) |
| 함께 고칠 문서 | SNAP-9(기간 기준으로 개정) · storage-and-subscription-policy §1 의 "Free 원본 스냅 한도 2GB" 행 **대체됨** · backlog A-1 ⑥ · A-2(구독이 기간을 팔지 여부가 새 미결) |

### 이 결정이 남긴 후속 판단

- **구독 혜택으로 기간을 팔 것인가**(예정, 미확정). 그렇게 가면 연장인지 무제한인지, 티어별로
  다른지를 정해야 하고, 구독 상품의 축이 "용량"에서 "기간"으로 옮겨간다. **정해지기 전까지
  구현은 "전원 15일"만 가정한다** — 다만 만료 판정은 §6-2(계획 문서)대로 정책 값에서 유도하므로,
  나중에 사용자별로 기간이 달라져도 스키마를 다시 건드리지 않는다.
- **용량 한도(2GB)를 존치할 것인가.** 기간 만료가 누적을 대신 막으므로 평균 사용자는 2GB 에
  닿지 않는다(월 50편 × 6MB, 15일이면 약 150MB — §2.1 기준). 유지하면 사용자에게 설명할 규칙이
  둘이 되고 아직 없는 집행 로직도 만들어야 한다. **권장: 무료 티어의 경계를 기간으로 일원화하고
  용량 한도는 폐기하되, 단기 대량 업로드를 막는 남용 방지 상한은 별도 값으로 둔다**(값 미정).
  결정 전까지 SNAP-9 는 이 항목을 미결로 표시한다.
- **구독이 끝나면 그동안 보관하던 스냅을 어떻게 할 것인가.** storage-and-subscription-policy §1 이
  이미 "30일 유예 → 읽기 전용 → 90일 후 정리"를 정해 두었으므로 그 규칙을 따르되, 기간 축에도
  같은 모양이 맞는지는 구독 상품(A-2) 확정과 함께 확인한다.
- **만료 기준 시각** — `capturedAt`(촬영)이 아니라 **서버 업로드 시각**을 쓴다. 보관 비용이
  발생하기 시작하는 시점이 업로드이고, `capturedAt` 은 클라이언트가 보고하는 값이라 만료를
 무한정 미루는 데 쓰일 수 있다.

만료의 동작 방식(2단계 삭제, 만료된 스냅의 식별, 기간 단축 시 사전 알림)은
[plans/lifecycle-alignment.md](../plans/lifecycle-alignment.md) §6 에서 이미 설계됐다 —
이 결정은 그 구조 위에 "무료 = 15일" 이라는 값을 얹는 것이다.
