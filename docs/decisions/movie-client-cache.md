# 무비의 앱 측 저장 — 서버 캐시 + 아웃박스, 앱이 정한 id

**작성일**: 2026-09-12
**상태**: 결정 — 구현 완료(앱·API). 실기기 검증은 [backlog.md](../backlog.md) A-1
**관련**: [movie-model.md](./movie-model.md) · [movie-export-policy.md](./movie-export-policy.md) ·
[movie-cleanup-after-export.md](./movie-cleanup-after-export.md) ·
[archive/movie-server-transition.md](../archive/movie-server-transition.md)(착수 계획) ·
앱 동작 [apps/mobile/docs/features/movie.md](../../apps/mobile/docs/features/movie.md)

---

## 무엇을 정했나

1. **앱의 무비 스토어는 서버 무비의 캐시 + 아웃박스다.** 화면은 전과 같이 zustand 스토어를 읽고 쓰며
   즉시 반영된다. 쓰기는 무비를 `pending`(`create`/`update`) 으로 표시하고, 동기화 워커
   (`features/compose-movie` 의 `MovieSyncGate`)가 **컷의 스냅이 모두 업로드된 뒤** 서버로 보낸다.
   읽기는 로그인 시와 포그라운드 복귀 시 `GET /movies` 전 페이지를 받아 병합한다 — pending 무비는
   로컬을, 나머지는 서버를 취한다.
2. **무비 id 는 앱이 uuid 로 정하고 서버가 그대로 받는다.** `POST /movies` 의 `id`(선택). 같은 id
   재전송은 멱등(있는 것을 돌려준다), 다른 사용자의 id 와 겹치면 409.
3. **`Movie.jobId` 를 응답에 노출한다.** 진행률·취소·실패 사유는 편집 작업 API 로 보는데, 앱이 `export`
   응답의 jobId 를 재시작으로 잃으면 닿을 길이 없기 때문이다.
4. **`PATCH /movies/{id}` 의 `clips` 는 빈 배열을 받는다.** 마지막 스냅을 지운 무비도 초안으로 남는다.
5. **취소된 작업은 무비를 `draft` 로 되돌린다**(결과물 포인터도 비운다). 앱이 취소를 초안으로 다루므로
   서버가 `failed` 라 하면 두 쪽이 서로 고치려 든다.
6. **완료 알림은 서버 푸시만**, 실패 알림은 앱 로컬만. 알림 스위치가 서버 판정에 닿지 않는 틈은 B-6.
7. **기존 로컬 무비는 이관하지 않는다**(개발 단계 — 2026-09-09 결정 그대로). 스토어 `version: 1` 마이그레이션이
   파일을 비운다.

## 왜 캐시 + 아웃박스인가 (TanStack Query 가 기본인데)

앱 규칙은 서버 데이터를 `queryOptions` 팩토리로 두라고 한다. 무비는 예외로 두었다:

- **무비는 서버가 받을 수 있기 전에 존재해야 한다.** 촬영 직후 스냅을 담아 만든 초안의 컷은 아직
  `videoId` 가 없다. 서버는 컷 목록을 통째로만 받으므로 그 무비를 서버에 만들 수 없고, 사용자는 그동안
  편집·이름 변경·삭제를 할 수 있어야 한다. 쿼리 캐시 위의 낙관적 업데이트로는 "서버에 없는 상태로
  며칠 편집되는 객체"를 표현할 수 없다.
- **오프라인 편집이 살아야 한다.** 스냅 라이브러리가 로컬 우선이라 무비도 그래야 한다. 영속 스토어가
  아웃박스를 재시작 너머로 보존한다.
- **소비자 34개 파일이 스토어 훅 계약에 의존한다.** 훅을 유지하면 화면을 건드리지 않는다.

기각한 대안:

| 대안 | 기각 이유 |
|---|---|
| (b) 초안은 로컬, 첫 export 때 서버 생성 | 원천이 둘로 남아 "기기를 바꾸면 초안이 사라진다"가 계속. 초안이 제품의 실제 바구니(트레이 대체)다 |
| (c) 서버 계약에 "업로드 대기 컷" 표현 추가 | 서버가 로컬 파일명을 알게 된다. 스냅 원천 전환(A-4)과 정면 충돌 |
| 임시 id → 서버 id 교체 | 사용자가 이미 열어 둔 경로·알림의 `movieId`·렌더 캐시 키가 바뀐다. 매핑 테이블을 앱 전역에 두어야 한다 |
| 서버 상태를 앱이 직접 갱신(PATCH status) | 워커가 무비를 모르는 현 구조에서 서버가 읽기 시 작업으로 상태를 따라잡는 편이 단순하고, 앱의 낡은 상태가 서버를 덮어쓰지 않는다 |

## 읽기 병합 규칙 (`entities/movie/lib/movie-sync.ts`)

- pending 무비는 로컬 그대로. 그 외는 서버 값을 취하되, **이 기기만 아는 것**(렌더 파일 주소·커버, 진행 중
  작업의 진행률, 실패 문구, `bgm`)은 서버가 같은 결과물/작업을 가리키는 동안 유지한다.
- 서버가 `generating` 이거나, 이 기기가 본 적 없는 결과물로 `ready`/`failed` 이면 **`adopted` 작업을 붙여
  `generating` 으로 되돌리고** 기존 러너가 따라잡게 한다(결과물 조회·실패 문구 생성 경로를 재사용).
  `adopted` 작업의 종료는 알리지 않는다. 이 기기가 이미 결론 낸 작업(`settledJobId`)은 다시 붙이지 않는다.
- 서버에 없는 무비는 `create` 대기 중인 것만 남기고 버린다. 서버가 잃은 무비의 삭제 대기는 정리한다.

## 남은 것

- 실기기 검증(전송·export·푸시·끝내기·재설치 후 읽기) — [backlog.md](../backlog.md) A-1.
- 서버 목록 동기화 3단계(스냅 reconcile)가 없으면 재설치 뒤 무비는 돌아오지만 컷의 원본이 없다 — A-4.
- 두 기기가 같은 무비를 오프라인에서 고치면 무비 단위 last-writer-wins. 필드 병합은 만들지 않았다.
