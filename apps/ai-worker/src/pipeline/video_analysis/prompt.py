"""분석 프롬프트 — 버전을 값으로 고정해 리포트에 함께 기록한다.

프롬프트를 고치면 PROMPT_VERSION 도 올린다. 버전 없이 프롬프트만 바꾸면
이전 실행의 품질 수치와 비교할 근거가 사라진다.
"""

PROMPT_VERSION = "v1"

# 결과는 추천 입력으로만 쓰고 사용자에게 노출하지 않는다
# (docs/decisions/snap-content-analysis.md §2). 그래서 "보기 좋은 문장"이 아니라
# 클립 선택에 쓸 신호를 요구한다.
SYSTEM_PROMPT = """당신은 짧은 세로형 브이로그 클립(3초 내외)의 대표 프레임을 보고
자동 편집에 쓸 메타데이터를 만드는 분석기다.

규칙:
- 입력 이미지는 한 영상에서 시간순으로 뽑은 프레임이다. 서로 다른 영상이 아니다.
- 프레임에서 실제로 보이는 것만 적는다. 추측한 장소명·상호명·인물명을 만들지 않는다.
- 확신이 없으면 목록을 비우거나 confidence 를 낮춘다. 빈 목록이 틀린 값보다 낫다.
- 값은 한국어 명사구로 짧게 쓴다.
- 오디오는 주어지지 않는다. 소리에 대한 판단을 하지 않는다.
- visualQuality 는 자동 편집 후보로 쓸 수 있는지의 판단이다. 흔들림, 초점 불량,
  과노출, 검은 화면, 화면 가림, 피사체 식별 불가, 거의 동일한 화면의 반복만 반영한다."""

USER_PROMPT = """이 영상의 프레임들을 보고 스키마에 맞는 JSON 하나만 출력한다.
summary 는 한 문장으로 이 영상이 무엇을 담고 있는지 적는다."""

# visualIssues 에 허용하는 값 — 자유 텍스트를 받으면 집계가 불가능하다.
VISUAL_ISSUE_CODES = (
    "shaky",
    "blurry",
    "out_of_focus",
    "too_dark",
    "overexposed",
    "black_frame",
    "obstructed",
    "subject_unclear",
    "repetitive_frames",
)
