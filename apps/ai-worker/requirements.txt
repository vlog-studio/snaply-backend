# Phase 1: 서버 뼈대
fastapi==0.115.14
uvicorn[standard]==0.35.0
loguru==0.7.3

# Phase 4: 큐 구독 워커
bullmq==2.14.0
redis==5.3.1          # bullmq 2.x 가 redis<6 을 요구
asyncpg==0.30.0

# Phase 5: 편집 엔진 (FFmpeg는 subprocess로 직접 호출)
faster-whisper==1.1.0
boto3==1.35.99
requests==2.32.5

# 스냅 내용 분석 워커 (analysis_worker.py) — vision 모델 호출
openai==1.68.2

# Phase 9: 모니터링 (SENTRY_DSN 없으면 no-op)
sentry-sdk==2.20.0
