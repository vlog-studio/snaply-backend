# Generated by `npm run openapi:write -w apps/api` from the Zod contract in
# packages/shared-types, and guarded by test/openapi-snapshot.test.ts. Collapsed
# in GitHub reviews because it is machine-produced, but its diff stays readable —
# this file *is* the backend contract, so a spec change is the thing worth reviewing.
apps/api/openapi.json linguist-generated=true
